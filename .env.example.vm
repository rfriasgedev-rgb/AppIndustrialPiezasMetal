# Variables de entorno para el archivo .env de producción
# COPIAR ESTE ARCHIVO A: .env (en la raíz del proyecto)
# NUNCA SUBIR .env al repositorio

# ── Base de datos ──────────────────────────────────────────
DB_ROOT_PASSWORD=RootChangeMe123!
DB_USER=metalerp
DB_PASSWORD=ChangeMe123!
DB_NAME=metal_parts_db

# ── JWT ───────────────────────────────────────────────────
# Generar con: openssl rand -hex 32
JWT_SECRET=REEMPLAZAR_CON_CLAVE_ALEATORIA_DE_64_CHARS
JWT_EXPIRES_IN=8h
